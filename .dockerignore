.git
.github
venv
.venv
__pycache__
*.pyc
.ansible
collections
*.retry
.idea
.vscode
.DS_Store

# Runtime mount surfaces — never bake into the image.
# inventory/hosts.yml is provided by a Kubernetes ConfigMap (or a local
# bind mount); inventory/group_vars/all/vault.yml is provided by a
# Kubernetes Secret (or a local bind mount).
inventory

# Not needed at runtime inside the container.
docs
LICENSE
README.md
.yamllint.yml
Dockerfile
.dockerignore
